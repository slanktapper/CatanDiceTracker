name: catan_dice_tracker
description: A Flutter app with bar graph showing values 2-12
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.65.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true